<table class="table table-hover">
  <thead>
  <tr>
    <th>Name</th>
    <th>Average Rating</th>
  </tr>
  </thead>

  <tbody>
  <% c.each do |item| %>
      <tr>
        <td><%= link_to item %></td>
        <td><%= round(item.average_rating) %></td>
        <td></td>
      </tr>
  <% end %>
  </tbody>
</table>