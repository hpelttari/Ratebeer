<%= form_with(model: beer_club, local: true) do |form| %>
  <% if beer_club.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(beer_club.errors.count, "error") %> prohibited this beer_club from being saved:</h2>

      <ul>
      <% beer_club.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <!--
  <div class="form-group">

  <label for="name">Name</label>
  <input type="name" class="form-control" id="name">

  </div>

  <div class="form-group">

  <label for="founded">Founded</label>
  <input type="founded" class="form-control" id="founded">

  </div>

  <div class="form-group">

  <label for="city">City</label>
  <input type="city" class="form-control" id="city">

  </div>

  <button type="submit" class="btn btn-primary">Create Beer Club</button>
-->
<div class="form-group">
  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, class:"form-control" %>
  </div>

  <div class="field">
    <%= form.label :founded %>
    <%= form.number_field :founded, class:"form-control" %>
  </div>

  <div class="field">
    <%= form.label :city %>
    <%= form.text_field :city, class:"form-control" %>
  </div>

  <div class="actions">
    <%= form.submit class:"btn btn-primary" %>
  </div>
  </div>
<% end %>
